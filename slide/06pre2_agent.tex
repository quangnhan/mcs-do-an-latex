\begin{frame}{AI Agent: LLM điều phối công cụ để hiểu, lập kế hoạch và thực thi}
  \centering
  \vfill
  \includegraphics[width=\linewidth,height=0.80\textheight,keepaspectratio]{ai_agent.png}
  \vfill
  {\footnotesize Agent dùng LLM làm bộ não để phân rã yêu cầu người dùng thành danh sách tác vụ,
  rồi gọi các công cụ (truy hồi tri thức, mô hình ML, trình thực thi) nhằm tạo ra kết quả bám ngữ cảnh.}
\end{frame}
