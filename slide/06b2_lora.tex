\begin{frame}[t]{LoRA: chỉ huấn luyện một phần nhỏ thay vì toàn bộ mô hình}
  \begin{columns}[T,onlytextwidth]
    \begin{column}{0.38\textwidth}
      \begin{block}{Ý tưởng cốt lõi}
        \small
        \begin{itemize}
          \item \textbf{Đóng băng} trọng số gốc $W_0$
          \item Chỉ huấn luyện hai ma trận nhỏ $A$, $B$
          \item Số tham số giảm \textbf{10--100 lần}
          \item QLoRA = LoRA + Quantization 4-bit
        \end{itemize}
      \end{block}
    \end{column}
    \begin{column}{0.58\textwidth}
      \centering
      \vspace{0.2cm}
      \includegraphics[width=\linewidth]{LoRA.png}
      \\[0.3em]
      {\footnotesize \textit{Trái: cập nhật toàn bộ $W$. \quad Phải: chỉ huấn luyện $A$, $B$ -- $W_0$ đóng băng.}}
    \end{column}
  \end{columns}
\end{frame}
