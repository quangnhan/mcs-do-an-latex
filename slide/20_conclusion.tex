\begin{frame}[t]{Hướng phát triển}
  \begin{columns}[T,onlytextwidth]
    \begin{column}{0.47\textwidth}
      \begin{block}{Cải thiện pipeline học}
        \small
        \begin{itemize}
          \item Thu thập $\mathcal{D}_{\text{pref}} = \{(x, y^+, y^-)\}$ từ vòng duyệt bài
                $\rightarrow$ fine-tune với DPO trực tiếp trên F\&B
          \item Thiết kế reward đa chiều $r = w_F F + w_E E + w_V V$
                thay vì 1 scalar cross-domain
          \item Validate $\text{corr}(r,\,\text{human})$ để đảm bảo
                reward model phản ánh đúng sở thích thực
        \end{itemize}
      \end{block}
    \end{column}
    \begin{column}{0.47\textwidth}
      \begin{block}{Mở rộng hệ thống}
        \small
        \begin{itemize}
          \item Scale lên 7B+ khi tài nguyên cho phép;
                thử nghiệm multimodal (ảnh + caption cùng lúc)
          \item Mở rộng sang Instagram, TikTok, Zalo OA
          \item Triển khai on-premise thực tế với một SME F\&B
                để thu thập phản hồi người dùng thực
        \end{itemize}
      \end{block}
    \end{column}
  \end{columns}

  \vspace{0.5cm}
  \centering
  \begin{tikzpicture}[node distance=1.1cm, every node/.style={font=\small}]
    \node[draw, rounded corners, fill=softblue,
          text width=2.5cm, minimum height=1.0cm, align=center] (sft) {SFT+DPO\\{\footnotesize hiện tại}};
    \node[draw, rounded corners, fill=softgreen,
          right=of sft, text width=2.8cm, minimum height=1.0cm, align=center] (pref) {Preference\\$\mathcal{D}_{\text{pref}}$ F\&B};
    \node[draw, rounded corners, fill=softorange,
          right=of pref, text width=2.8cm, minimum height=1.0cm, align=center] (dpo) {Training với DPO};
    \node[draw, rounded corners, fill=softred,
          right=of dpo, text width=2.5cm, minimum height=1.0cm, align=center] (deploy) {Triển khai\\SME thực};
    \draw[-{Stealth}, thick] (sft)   -- (pref);
    \draw[-{Stealth}, thick] (pref)  -- (dpo);
    \draw[-{Stealth}, thick] (dpo)   -- (deploy);
  \end{tikzpicture}
\end{frame}
