\begin{frame}[t]{SFT: dạy mô hình học từ ví dụ đã kiểm duyệt}
  \begin{columns}[T,onlytextwidth]
    \begin{column}{0.52\textwidth}
      \begin{block}{SFT là gì?}
        \small
        \begin{itemize}
          \item Cho model học từ các cặp \textit{(prompt, câu trả lời tốt)} đã được kiểm duyệt
          \item Giống dạy kèm: model thấy đầu vào, đầu ra mẫu rồi học cách bắt chước
          \item Sau SFT, model viết đúng phong cách, đúng ngữ cảnh bài toán cụ thể
        \end{itemize}
      \end{block}
    \end{column}
    \begin{column}{0.44\textwidth}
      \centering
      \begin{tikzpicture}[node distance=0.8cm, every node/.style={font=\small}]
        \node[draw, rounded corners, fill=softblue,
              text width=3.2cm, minimum height=1.0cm, align=center] (data)
          {Dữ liệu đã kiểm duyệt\\{\footnotesize (prompt, câu trả lời tốt)}};
        \node[draw, rounded corners, fill=softgreen,
              below=of data, text width=3.2cm, minimum height=1.0cm, align=center] (sft)
          {SFT + QLoRA};
        \node[draw, rounded corners, fill=softorange,
              below=of sft, text width=3.2cm, minimum height=1.0cm, align=center] (out)
          {Model chuyên biệt};
        \draw[-{Stealth}, thick] (data) -- (sft);
        \draw[-{Stealth}, thick] (sft) -- (out);
      \end{tikzpicture}
    \end{column}
  \end{columns}
\end{frame}
