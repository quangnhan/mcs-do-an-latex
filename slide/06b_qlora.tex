\begin{frame}[t]{SFT + QLoRA: dạy lại mô hình với dữ liệu F\&B, chi phí thấp}
  \begin{columns}[T,onlytextwidth]
    \begin{column}{0.48\textwidth}
      \begin{block}{SFT -- Supervised Fine-Tuning}
        \small
        \begin{itemize}
          \item Cho model học từ các cặp \textit{(prompt, bài đăng tốt)} đã được kiểm duyệt
          \item Giống như dạy kèm: model thấy đầu vào và đầu ra mẫu, học cách bắt chước
          \item Sau SFT, model biết viết đúng phong cách F\&B Việt Nam
        \end{itemize}
      \end{block}
    \end{column}
    \begin{column}{0.48\textwidth}
      \begin{block}{QLoRA -- Tiết kiệm bộ nhớ}
        \small
        \begin{itemize}
          \item Full fine-tuning đòi hỏi GPU rất lớn
          \item QLoRA chỉ thêm một lớp tham số nhỏ vào model gốc (rank thấp)
          \item Model gốc được giữ nguyên, chỉ huấn luyện phần bổ sung
          \item Kết quả: train được mô hình 4B--9B trên GPU thông thường
        \end{itemize}
      \end{block}
    \end{column}
  \end{columns}

  \vspace{0.5cm}
  \centering
  \begin{tikzpicture}[node distance=0.8cm, every node/.style={font=\small}]
    \node[draw, rounded corners, fill=softblue,
          text width=3.2cm, minimum height=1.0cm, align=center] (data)
      {Dữ liệu F\&B đã kiểm duyệt};
    \node[draw, rounded corners, fill=softgreen,
          right=0.8cm of data, text width=3.2cm, minimum height=1.0cm, align=center] (train)
      {Fine-tune (QLoRA)};
    \node[draw, rounded corners, fill=softorange,
          right=0.8cm of train, text width=3.2cm, minimum height=1.0cm, align=center] (model)
      {Agent chuyên biệt F\&B};
    \draw[-{Stealth}, thick] (data) -- (train);
    \draw[-{Stealth}, thick] (train) -- (model);
  \end{tikzpicture}
\end{frame}
