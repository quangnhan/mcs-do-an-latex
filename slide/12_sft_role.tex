\begin{frame}[t]{SFT: dễ train, hiệu quả --- nhưng không tối ưu sở thích}
  \begin{columns}[T,onlytextwidth]
    \begin{column}{0.48\textwidth}
      \begin{block}{Ưu điểm}
        \scriptsize
        \begin{itemize}
          \item Ổn định: MLE trực tiếp trên dữ liệu có nhãn, không cần reward model
          \item Chỉ cần 500 mẫu kiểm duyệt để học phong cách F\&B
          \item 4B+SFT Overall $0{,}687$ --- chỉ cách GPT-5.4-mini $2{,}8\%$
        \end{itemize}
      \end{block}
    \end{column}
    \begin{column}{0.48\textwidth}
      \begin{block}{Giới hạn}
        \scriptsize
        \begin{itemize}
          \item Chỉ học phân phối dữ liệu kiểm duyệt, không tối ưu sở thích
          \item Không có cơ chế phân biệt ``tốt hơn'' giữa hai output đều chấp nhận được
          \item Cần dữ liệu sở thích để tiếp tục cải thiện
        \end{itemize}
      \end{block}
    \end{column}
  \end{columns}

  \vspace{0.5em}
  \centering
  \begin{tikzpicture}[
    node distance=1.2cm,
    every node/.style={font=\small},
    box/.style={draw, rounded corners, text width=3.0cm, minimum height=1.1cm, align=center}
  ]
    \node[box, fill=softblue] (sft) {\textbf{SFT}\\Học từ nhãn có sẵn};
    \node[box, fill=softorange, right=of sft] (pref) {\textbf{Preference data}\\Gán nhãn sở thích};
    \node[box, fill=softgreen, right=of pref] (dpo) {\textbf{DPO}\\Tối ưu sở thích};
    \draw[-{Stealth}, thick] (sft) -- (pref);
    \draw[-{Stealth}, thick] (pref) -- (dpo);
  \end{tikzpicture}

  \eqblock{%
    $\mathcal{L}_{\text{SFT}} = -\mathbb{E}_{(x,y^*)}\bigl[\log\pi_\theta(y^*\mid x)\bigr]$:
    tối ưu xác suất output ``đủ tốt'', không tối ưu ``tốt hơn lựa chọn khác''.
  }
\end{frame}
